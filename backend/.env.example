# Base Settings
NODE_ENV=local
PORT=9000
API_PREFIX=api

# DB config
DB_ENGINE=mariadb
DB_HOST=localhost
DB_PORT=3312
DB_USER=root
DB_PASS=root
DB_NAME=test_db

# =============================================================================
# S3 Configuration
# =============================================================================
# You have two options:
#
# OPTION 1: LocalStack (recommended for local development)
# --------------------------------------------------------
# Just set S3_ENDPOINT and use dummy credentials. The bucket is created automatically.
#
#   S3_ENDPOINT=http://localstack:4566
#   AWS_ACCESS_KEY=test
#   AWS_SECRET_KEY=test
#   AWS_BUCKET_REGION=us-east-1
#   AWS_BUCKET_NAME=demo-bucket
#
# OPTION 2: Real AWS S3
# --------------------------------------------------------
# Leave S3_ENDPOINT empty and fill in your real AWS credentials.
#
#   S3_ENDPOINT=
#   AWS_ACCESS_KEY=your-access-key
#   AWS_SECRET_KEY=your-secret-key
#   AWS_BUCKET_REGION=us-east-1
#   AWS_BUCKET_NAME=your-bucket-name
#
# =============================================================================

S3_ENDPOINT=http://localstack:4566
S3_PUBLIC_ENDPOINT=http://localhost:4566
AWS_ACCESS_KEY=test
AWS_SECRET_KEY=test
AWS_BUCKET_REGION=us-east-1
AWS_BUCKET_NAME=demo-bucket