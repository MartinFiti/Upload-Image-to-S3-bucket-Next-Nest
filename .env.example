# Database Configuration
DB_USER=root
DB_PASS=root
DB_NAME=test_db
DB_PORT=3012

# Backend
PORT=9000
NODE_ENV=local
API_PREFIX=api

# =============================================================================
# S3 Configuration (LocalStack - default for demo)
# =============================================================================
# LocalStack provides a local S3 emulator for development.
# NOTE: LocalStack images don't persist between restarts.
#       For persistent storage, use real AWS S3 (see below).
#
# S3_ENDPOINT: Internal Docker network URL (backend connects here)
# S3_PUBLIC_ENDPOINT: Browser-accessible URL (pre-signed URLs use this)
#
# For real AWS S3, set both endpoints to empty and fill AWS credentials.
# =============================================================================
S3_ENDPOINT=http://localstack:4566
S3_PUBLIC_ENDPOINT=http://localhost:4566
AWS_ACCESS_KEY=test
AWS_SECRET_KEY=test
AWS_BUCKET_REGION=us-east-1
AWS_BUCKET_NAME=demo-bucket